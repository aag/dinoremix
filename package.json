{
  "name": "dinosaur-remix",
  "version": "0.1.0",
  "author": "Adam Goforth",
  "description": "A web app for remixing Dinosaur Comics",
  "license": "GPL-2.0",
  "dependencies": {
    "minifier": "^0.8.1"
  },
  "scripts": {
    "build": "minify --template {{filename}}-{{md5}}.min.{{ext}} --clean public/assets/ && rm -f public/assets/dist/* && mv public/assets/*-*.min.* public/assets/dist/"
  }
}
